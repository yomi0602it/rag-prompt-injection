# AWS Credentials (configured via AWS CLI or IAM Roles in production)
AWS_REGION="us-east-1"

# Vector Database (Output from Terraform apply)
OPENSEARCH_ENDPOINT="https://[YOUR-ID].us-east-1.aoss.amazonaws.com"